import { useState } from 'react';

export default function AnonymousLoginReminder() {
  return <div id="anonymous_login_reminder" className="h-8 bg-blue-800 text-white flex justify-center items-center">
    <span>Log in or register to save your uploaded apps.</span>
  </div>
}